# VITE_API_URL=https://backend-mentor.onrender.com
# Uncomment the line above to test against the production Render backend.
# Right now it is commented out so the App falls back to local http://localhost:5000